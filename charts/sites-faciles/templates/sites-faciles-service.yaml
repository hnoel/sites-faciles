apiVersion: v1
kind: Service
metadata:
  name: sites-faciles
spec:
  selector:
    app: sites-faciles
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: {{ .Values.service.type }}
